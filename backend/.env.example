# Server Configuration
PORT=5001
NODE_ENV=production

# Frontend URLs (for CORS)
FRONTEND_URL=https://ddos-spec.github.io
ALLOWED_ORIGINS=https://ddos-spec.github.io,http://localhost:3000

# Database Configuration
DATABASE_URL=postgresql://user:password@postgres-scrapdatan8n-dbgate.qk6yxt.easypanel.host:5432/wa_gateaway
DB_HOST=postgres-scrapdatan8n-dbgate.qk6yxt.easypanel.host
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=wa_gateaway

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# WhatsApp Session Configuration
SESSION_PATH=./sessions

# Webhook Configuration (optional)
WEBHOOK_BASE_URL=
